# Local database connection parameters
spring.datasource.url = jdbc:postgresql://localhost:5432/tejko-games
spring.datasource.username = postgres
spring.datasource.password = 123456
spring.datasource.driverClassName = org.postgresql.Driver

# Jpa configuration parameters
spring.jpa.generate-ddl = false
spring.jpa.show-sql = false 
spring.jpa.database-platform = org.hibernate.dialect.PostgreSQL82Dialect
spring.jpa.hibernate.ddl-auto = update

# JSON Web Token parameters
matej.tejkogames.jwtSecret = tajna
matej.tejkogames.jwtExpirationMs = 86400000

# General parameters
spring.origin = "http://localhost:3000"
